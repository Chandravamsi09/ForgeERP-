{
  "name": "forge-erp",
  "version": "2.0.0",
  "private": true,
  "description": "Tier-1 Manufacturing Enterprise Resource Planning (ERP) Platform",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix shared run build && npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix backend test",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc -b shared backend frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.9.3"
  }
}
